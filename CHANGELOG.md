# 变更记录

## 2026-08-17 词汇积累分页与资源管理优化

### 资源列表取数与词汇积累

- 修复 `resources.listPublic` / `resources.list` 未分页导致 uniCloud 默认只返回 100 条的问题，改为分批取全量；`batchCreateVocabulary` 去重也改为读取全部已存在标题。
- `resources` 云对象取全量改为“先 count 再并行分页”，减少大量词汇资源加载耗时；网页端与小程序本地云对象副本同步。
- 词汇积累页改为每页 50 条，支持上一页/下一页/页码跳转，切换语言或卡片时回到第 1 页，翻页自动回到页面顶部。
- 词汇查询按当前语言传给云端，避免拉取其他语言数据；页面只渲染当前页，避免 1840 条词汇一次性进入 DOM。

### 管理端资源管理

- 学习资源管理列表改为每页 50 条，支持页码跳转；搜索或类型筛选后回到第 1 页，删除数据后自动修正当前页。
- 翻页自动滚动到“学习资源管理”区块顶部。
- 资源表格“语言”列只在筛选“词汇”时显示，视频/阅读/听力/全部不再显示该列。
- 注意：`resources` 云对象需重新部署到共享空间后生效。

## 2026-08-17 导航栏配色统一、学习页面改版与词汇批量导入

### 导航栏配色统一

- 用户端全部带侧栏页面的导航栏统一为学习中心基准样式：激活菜单纯色 `--rule-primary` 白字（hover 用 `--rule-primary-hover`）、logo 纯色、头像浅蓝底 `--rule-primary-tint-1`、菜单文字与退出登录用 `--rule-ink-2`。
- 修正 `video-list`、`legal-vocab`、`reading-list` 三个页面残留的渐变激活态/渐变头像/渐变 logo，并补充 `--rule-ink-2` 变量。
- 管理端 5 个页面保留原有渐变设计体系，本次不动。

### 学习页面重新设计

- 文本阅读页 `reading-list.vue` 改版：新增深蓝渐变 Hero 横幅（含统计与浮动书籍装饰）、分类/难度/字数筛选与搜索、六色分类图标卡片、展开阅读与"打开原文"外链、加载/空状态美化。
- 听力训练页 `listening-training.vue` 改版：Hero 新增耳机装饰与均衡器动画（播放时跳动）、本周任务/已完成/平均正确率三项统计、播放器标题栏小号 EQ 动画；修复任务难度徽章配色（按 meta 自动映射初级/中级/高级）；移动端 Hero 纵向布局。

### 今日词汇按天轮换

- 法律英语"今日词汇"由"每天随机抽取 + 复习优先"改为按天轮换制：固定种子将词库排成稳定队列，每天沿队列滑动 12 个（10-15 区间），连续多天批次不重复，优先取未学词、不足时补充已学词；当天内刷新结果稳定。

### 词汇批量导入

- 新增 `resources.batchCreateVocabulary` 云对象方法：按 title 去重（忽略已存在词汇）、批量写入、单次上限 2000 条。
- 管理端资源管理新增"批量导入词汇"卡片（仅词汇类型显示）：支持粘贴文本或选择 txt 文件，格式为"英文词 + 空格/Tab + 中文释义（可选第三列分类）"，导入语言随上方"语言分类"选择（英语/德语/法语/拉丁语/西班牙语），导入即上线。
- 解析《法律英语词汇大全》生成 1590 条词汇导入数据 `词汇导入_法律英语词汇大全.txt`，覆盖律师/诉讼/民事/商事/刑法/劳动/行政/其他 8 大分类，导入前需重新部署 `resources` 云对象。

### 文档更新

- `README.md` 重写：删除"后端交接"章节（服务空间权限交接与交接清单），"如何上线视频"改为后台直传云存储新流程，移除过时的本地视频服务器说明。
- 资源库填充操作指南补充"批量导入词汇"章节。

## 2026-08-16 词汇按语言分类与资源 lang 字段

- 网页端新增 `pages/learning-center/vocab-language.vue` 语言分类页：`法律英语 -> 词汇积累` 先展示英语、德语、法语、拉丁语、西班牙语五张卡片，并统计各语言已上线词汇数。
- `legal-vocab.vue` 支持 `?lang=` 参数过滤，页面内可切换语言；旧词汇数据没有 `lang` 时默认按“英语”展示。
- `resource.schema.json` 新增 `lang` 字段，枚举为英语/德语/法语/拉丁语/西班牙语；`resources` 云对象新增 `lang` 参数、写入校验和更新允许字段。
- 管理端资源管理新增词汇“语言分类”选择，新增/编辑/保存/搜索支持语言；资源表格新增语言列，非词汇资源显示 `--`。
- 法律英语页“今日词汇”只读取英语词汇，避免混入其他语言的资源。
- `resources` 云对象已同步到小程序本地 `cloudfunctions` 副本。
- 注意：`resource.schema.json` 与 `resources` 云对象尚未部署到共享空间；小程序端目前还没有词汇积累页面，本次只完成了网页端前端和共享云对象改动。

## 2026-08-14 资源库升级为四类资源模型

- `resource` 资源类型由 `video/english` 升级为 `video/vocabulary/reading/listening` 四类。
- `resource.schema.json` 新增 `audioUrl`、`content`、`questions`、`sortOrder`、`level`、`tags` 字段；`type` 枚举改为 `video/vocabulary/reading/listening`。
- `resources` 云对象新增四类资源新增/更新校验，支持 `category`、`keyword` 筛选，列表按 `sortOrder` 升序、`createDate` 降序返回；网页端与小程序本地副本已同步。
- 网页端资源管理后台改为统一资源列表，新增表单支持视频、词汇、文本阅读、听力训练四种动态录入；支持类型筛选、标题搜索、排序号维护和听力题目动态编辑。
- 删除资源录入表单下方无实际功能的“资源文件说明”空盒子。
- 网页端学习中心接入新模型：词汇页读取 `vocabulary`，新增 `reading-list` 文本阅读页，听力训练页读取 `listening` 并渲染音频、原文和题目；法律英语页学习模块按四类资源生成。
- 小程序端同步新模型：`resources` 本地副本更新，法律英语、首页推荐、听力训练接入新类型，新增 `legal-english/reading-list` 文本阅读页。
- 资源库操作指南补充四类资源录入说明，并明确不使用 B站等外部视频。
- 当前 `resource.schema.json` 与 `resources` 云函数尚未部署到共享空间；存在旧 `type=english` 数据时，上线前需迁移或清理。

## 2026-08-14 学习中心词汇功能与本地 AI 配置收口

- 同步 GitHub `jacoblu/master` 提交 `4b40b30`（2026-08-13）中的 17 个文件到本地 main，覆盖双端 AI 助手、登录注册、资源/用户管理、视频详情等文件。
- `aiChat` 云函数改为每次请求实时读取 `config.local.json`，避免 HBuilderX 本地调试启动后读不到 Key；网页端 `.gitignore` 忽略 `**/aiChat/config.local.json`。
- 网页端学习中心移除“法律英语资源”区块，保留“专业技能提升”中的法律英语综合训练入口。
- 法律英语“今日词汇”改为从资源库“词汇积累”分类按日期随机抽取 10 词，支持新词/待复习切换，并按 `1/2/4/7/15/30` 天间隔安排复习。
- 新增 `utils/vocab.js` 共享词汇状态工具，词汇进度与收藏保存在浏览器 Local Storage，按账号隔离。
- 新增 `pages/learning-center/legal-vocab.vue` 词汇积累页，展示“未学习词汇 / 待复习词汇 / 收藏词汇”三张卡片，支持收藏、认识、不认识、已掌握、再复习。
- “学习模块-词汇积累”卡片接入新页面，无词汇资源时保留入口并显示维护提示。

## 2026-08-13 小程序端接入真实 AI

- 小程序端复制 `aiChat` 云函数（智谱 GLM-4-Flash-250414），与网页端共用同一 AI 服务，支持本地运行调试；API Key 存放于 `config.local.json` 并在 `.gitignore` 中忽略。
- 网页端和小程序端 AI 助手移除 mock 问答，改为真实调用 `aiChat.chat`，保留对话上下文；AI 回答按 Markdown 排版渲染，思考中显示实时秒数。
- 网页端和小程序端视频详情页课程简介改为 AI 生成，内容为「课程概述」「你将学到」两部分，带本地缓存（版本号 v3）避免重复请求，生成中显示加载提示与「AI 生成」标记。
- 网页端视频详情页课程简介精简为「课程概述」「你将学到」两个部分，移除「适合人群」「课程亮点」；简介缓存升级版本号，旧缓存自动作废并重新生成。
- 网页端视频播放器卡片固定自身高度（16:9 视频 + 控制条），不再随课程简介卡片内容增高而被拉伸。
- 网页端课程简介卡片最高与播放器等高，内容超高时在卡片内部滚动查看；窗口缩放、字体加载完成后高度自动联动。

## 2026-08-12 资源导入、视频列表与 B站播放支持

- 管理端“录入学习资源”新增视频分类/资源类型字段，并明确封面 URL 用途。
- 资源编辑弹窗支持一次修改标题、分类、时长/难度、地址、封面、日期、简介和审核状态。
- `resources` 云对象允许更新 `date`，并增加审核状态合法性校验；网页端与小程序本地副本同步。
- 学习中心视频学习只展示最新 4 个视频，“更多视频”接入新的全部视频页。
- 新增网页端 `video-list` 全部视频页，支持搜索、分类筛选并跳转视频详情。
- “为你推荐”改为只推荐 4 个视频，新增刷新按钮，按随机替换逻辑更换推荐内容。
- 学习中心首页和全部视频页支持读取资源封面 URL，未填封面时保留渐变占位。
- 网页端视频详情支持 B站播放页地址和官方嵌入地址，自动切换为 iframe 播放。
- 修复直接打开视频详情页时资源未加载导致的空数据渲染报错。
- 重写资源库数据导入操作指南，补充云存储上传、URL 复制、管理端导入、上线验证、封面和 B站处理流程。

## 2026-08-09 静态数据与小程序真实数据收口

- 小程序测评接入 `questions.listPublic`，移除硬编码题目。
- 小程序交卷改为真实计分，并通过 `survey.saveResult` 保存测评结果。
- `survey` 云对象新增 `time` 字段，`survey_result.schema.json` 同步补充。
- 小程序数据中心、个人中心从 `survey.myResults` 读取真实测评记录。
- 小程序首页推荐、法律英语资源改为读取真实 `resources`。
- 小程序成就页、学习进度、听力训练等暂无数据模型，已清空为待接入状态。
- 网页端和小程序端视频详情移除默认示例视频回退，未配置 `fileUrl` 时提示缺少文件地址。
- 网页端专项选择页清空静态专项分类，待后续接入题目分类。
- 移除 `questions`、`resources`、`knowledge` 云对象中的自动种子数据。
- 修复题目库主观题 `subType` 丢失问题，并补上“论述题 / 案例分析题”切换。
- 修复问卷页题干字段读取错误，改为读取题目库的 `title`。

## 2026-08-08 云端框架与管理端

- 新增并完善 `user`、`resource`、`survey_result`、`question`、`legal_doc` 数据库 Schema。
- 新增并完善 `users`、`resources`、`survey`、`questions`、`knowledge` 云对象。
- 管理端题库、知识库、资源库、用户管理、数据总览接入真实数据。
- 网页端首页、登录、测评、结果、报告、个人中心、学习中心、法律库接入真实云数据。
- 小程序本地 `cloudfunctions` 同步 `users`、`resources`、`survey`、`questions`、`knowledge` 副本。

## 2026-08-06 初始版本

- 从原项目同步基础版本。
- 建立网页端与微信小程序端双端项目结构。
- 保留 uniCloud 云函数、数据库 Schema、页面与本地调试脚本。
